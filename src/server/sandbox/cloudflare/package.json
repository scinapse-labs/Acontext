{
	"name": "cloudflare-sandbox-worker",
	"version": "0.1.0",
	"description": "Cloudflare Sandbox Worker API proxy for Acontext",
	"main": "src/index.ts",
	"scripts": {
		"dev": "wrangler dev",
		"deploy": "wrangler deploy",
		"build": "tsc",
		"lint": "eslint ."
	},
	"dependencies": {
		"@cloudflare/sandbox": "^0.7.20"
	},
	"devDependencies": {
		"@cloudflare/workers-types": "^4.20260317.1",
		"typescript": "^6.0.2",
		"wrangler": "^4.77.0"
	},
	"engines": {
		"node": ">=18.0.0"
	},
	"pnpm": {
		"overrides": {
			"undici": ">=7.24.0"
		}
	}
}
